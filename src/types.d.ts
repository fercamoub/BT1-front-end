export interface Product {
  id: number;
  name: string;
  price: number;
  stock: number;
  category: string;
  expirationDate: string;
}
export interface ProductForm {
  name: string;
  price: number;
  stock: number;
  category: string;
  expirationDate: string;
}
